<%= form_with(model: pizza_order, local: true) do |form| %>
  <% if pizza_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pizza_order.errors.count, "error") %> prohibited this pizza_order from being saved:</h2>

      <ul>
      <% pizza_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :pizza_id %>
    <%= form.text_field :pizza_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
